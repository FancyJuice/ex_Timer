## Timer_v2 application release code
## How to test:
run UI.py directly

## How to deploy:
``` shell
pyinstaller -D -w -i logo.ico -n Timer UI.py
```
## Packages you need:
``` shell
pip install wx
pip install PIL
pip install pyinstaller
```
